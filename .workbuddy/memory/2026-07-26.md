# 2026-07-26

## 3D 飞机大战（雷霆空战）
- 完成纯静态 Web 游戏：`index.html` + `js/{main,world,plane,effects,audio,ai,net}.js`，Three.js r160 走 jsdelivr CDN importmap，用户将部署到 Cloudflare Pages。
- 玩法：机炮唯一武器；单一机型；HP 固定 100；弱点倍率（驾驶舱 x3 / 发动机 x2.5 / 机翼 x2 / 机身 x1）；五局三胜；第三人称追尾视角；鼠标转向、左键射击、右键变焦、W/S 加减速、M 静音；手机 UA 检测直接拦截提示。
- 联机：WebRTC 双 DataChannel（state 无序不可靠 20Hz / event 可靠有序）；本地权威结算——伤害由受击方本地扣血后广播。信令双轨：①6位房间码（公共 MQTT 代理 emqx/hivemq 仅握手，握手后纯 P2P，无 TURN/中转）；②备用手动 SDP 复制粘贴。
- 附带练习模式 AI（追击/规避/防坠海/边界回中）。
- 音效全部 Web Audio 合成（引擎、风噪、机炮、命中、爆炸、回合音乐），无外部资源。
- 全 7 模块 node --check 语法通过；修复过 ai.js 向量复用 bug。

## 2026-07-27 二次改进（用户反馈四项）
- **相撞平局**：双双坠毁 → 不加分，宽限期 0.45s 吸收网络抖动后判双死则平局。
- **敌机指示器**：HUD 红色菱形 + 实时距离，超出视锥时贴屏幕边缘。
- **6位房间码联机**：引入 mq​tt.js（unpkg 5.10.1）通过公共 MQTT broker 仅握手，游戏数据纯 P2P 无 TURN；实时显示 FPS + 延迟；保留手动信令为「备用」。
- **双地图 + 碰撞体积**：world.js 重写，海岛+废墟都市双地图，山/楼体全部带碰撞体（球/AABB+高度），擦撞扣血+推开+震屏；废墟都市含破损高楼、瓦砾、弹坑、烧毁楼顶火光+烟柱动效；主机选图广播给加入方。

## 2026-07-28 修 bug + 二级菜单
- **地图不显示根因**：world.js 重写后把物体放进 `root` Group 返回，但 main.js 的 `rebuildWorld` 漏了 `scene.add(world.root)`，整个地图没挂到场景树 → 补上这行修复。同时清理了 dispose 逻辑（旧的 for...in 遍历材质属性无效且重复 dispose）。
- **二级菜单**：去掉主菜单的下拉框，改为点入口 → 弹出地图卡片选择 → 继续。练习模式点卡片即开局；联机双方各自选图，连接后互发 `mapPref`，主机裁决（一致用该图 / 不一致随机），结果通过 `map` 事件广播给加入方。

## 2026-08-08 四次改进（改名苍穹对决 + 触屏适配）
- **游戏改名「苍穹对决 Sky Duel」**：新增 favicon.svg（蓝色战机剪影）、完整 SEO（description/keywords/og/JSON-LD VideoGame）、菜单美化（发光标题动画、机标 logo、毛玻璃面板），菜单阶段相机环岛巡航作动态背景。
- **选图挪到连接成功后**：房间面板只负责建房/输码；P2P 接通后双方进入「选图大厅」（15s 倒计时），各选一张互发 mapPref，主机裁决（一致用该图/不一致随机/超时用现有信息），map 事件广播给加入方。
- **转向限速**：鼠标/触屏输入改为写 targetYaw/targetPitch，实际 yaw/pitch 以 YAW_RATE=1.7 rad/s、PITCH_RATE=1.35 rad/s 逼近，杜绝甩鼠标瞬转。
- **手机端适配**：移除手机拦截；pointer:coarse 判定触屏模式；手游经典布局——左侧油门摇杆（上下推=加减速、松手保持）、空白区滑动转向、右下开火键、瞄准键；body.touch CSS 隐藏键鼠提示、调整 HUD 位置；lockPointer 在触屏下跳过。
- 联机相撞修正：net 模式不再本地替对手判死，等对方 died 进 0.45s 宽限期，保证两边比分一致。
- 7 模块 node --check 通过；HTML/JS 的 DOM id 交叉校验无缺失。

## 2026-08-09 五次改进（D1 匹配 + 多人 mesh + 本地化）
- **本地化资源**：下载 three@0.160.0 min 到 `vendor/three.module.min.js`，importmap 改本地路径；删除 mqtt.js（不再依赖任何 CDN）。
- **D1 后端**：新增 `schema.sql`（queue/matches/match_players/signals 四表）、`wrangler.toml`、`functions/api/{queue,signal,leave}.js` 三个 Pages Functions。queue.js 做匹配排队（1v1/3v3/5v5，凑齐人数成团、分两队、分配槽位）；signal.js 做 WebRTC SDP 中转（轮询读写 signals 表）；leave.js 清理退出。
- **删除旧联机代码**：移除 6 位房间码面板、手动 SDP 信令面板、hostRoom/joinRoom/MQTT 全部代码。
- **MatchClient（net.js）**：findMatch 轮询 /api/queue 直到拿到 roster；connectMesh 对 roster 中每个对手建独立 Net（RTCPeerConnection），按 peerId 字典序决定 initiator/responder，offer/answer 经 /api/signal 轮询交换（非 trickle ICE）。匹配成功后实时状态走 P2P mesh DataChannel（无 TURN）。
- **多人队伍 + IFF**：main.js 重写为 planes{} 字典管理 N 架飞机；同队蓝、敌队红，每架头顶挂队伍色光柱 Sprite（我方青/敌方红）随距离缩放；foeMarker 跟踪最近敌机；回合逻辑改为队伍淘汰制——一队全灭且 0.5s 宽限内对方也全灭则平局，否则该队负；1v1 是 N=2 的特例（含对撞平局）。
- **菜单**：四模式入口（练习/1v1/3v3/5v5）；匹配中显示搜索状态可取消；连接成功后主机选图广播，非主机等待。
- 全部模块 node --check 通过；HTML/JS DOM id 交叉校验无缺失；3 个 Function 文件语法通过。

## 2026-08-09 六次改进（修转向 + 恢复开房间 + 选图流程区分）
- **横滚反向 bug 修复**：plane.js applyFlight 里视觉滚转目标原本是 `-f.yawVel * 0.38`，符号写反导致右转时左滚；改为 `f.yawVel * 0.38`，右转→右翼下沉→右滚，符合现实。
- **恢复开房间模式**：上一轮 D1 改造误删了"创建房间/加入房间"入口，本轮恢复。新增 `functions/api/room.js`（D1 管理 6 位房间码 + 2 人握手信令中转，复用 matches/match_players/signals 三表）；net.js 新增 `RoomClient` 类（createRoom/joinRoom/waitForJoiner/connectPeer，主机=initiator）；main.js 加 `wireRoomClient` + 统一发送封装 `netBroadcast/netSendState/netSendEventTo`（按 mode 路由 matchClient 或 roomClient）。
- **选图流程区分**：
  - 练习模式：点入口→选图→直接开局（不变）
  - 开房间模式：建房/输码→P2P 接通→选图大厅（主机选图广播给对方）→开局
  - 匹配模式（1v1/3v3/5v5）：**匹配前先选图**（含"随机"选项）→进匹配队列→匹配成功后主机广播自己的选图给全员→全员 rebuildWorld+开局。匹配模式不再走选图大厅。
- 新增 HTML：roomEntryPanel/roomHostPanel/roomJoinPanel/matchPickPanel 四个面板 + matchPickMapChoice（含 random 卡片）。
- 7 个 JS 模块 + 4 个 Function 文件 node --check 全部通过；HTML↔JS DOM id 交叉校验无缺失。

## 2026-08-09 七次改进（练习/开房间加规模选项 + D1 部署文档）
- **练习模式加规模**：点练习→先选 1v1/3v3/5v5 规模→再选地图→开局。startPractice 改为接收 size 参数，按规模生成对应数量 AI（我方+敌方），roster 用假 peerId 填充保持结构一致；AI 追击逻辑改为找最近活敌机（玩家或其他 AI）。
- **开房间模式加规模**：点开房间→先选 1v1/3v3/5v5→再创建/加入。roomSize 变量记录规模。
- **D1 部署说明文档**：新增 `D1部署说明.md`（8 步：装 wrangler→登录→建库→填配置→建表→部署 Pages→控制台绑 D1→验证），含本地调试、常见问题、表结构说明。
- 外部资源确认：three.js 已本地化（vendor/），信令走 D1 无外部信令服务器，仅用 Google STUN 做 NAT 穿透不中转数据。
- 7 模块 node --check 通过；DOM id 交叉校验无缺失。

## 2026-08-09 修正：彻底移除 STUN
- 用户重申：纯 WebRTC 直连，不要任何外部服务器（包括 STUN）。
- net.js 的 RTC_CONFIG 从 `iceServers: [google stun]` 改为 `iceServers: []`，现在零外部服务器。
- D1部署说明.md 同步更新描述。

## 2026-08-09 修正：网页版上传支持
- 删除 wrangler.toml（网页上传器看到它就报"不支持需要构建过程的项目"）。
- D1部署说明.md 全面重写为纯网页版操作流程（建库→建表→上传→绑D1→验证），不依赖任何命令行工具。命令行方式保留在 FAQ 里作为可选。

## 2026-08-09 八次改进：D1→纯 PeerJS
- 用户 Pages 不支持 Functions，选择纯前端 PeerJS 方案。
- 删除 functions/、schema.sql、wrangler.toml、D1部署说明.md。
- 下载 peerjs@1.5.4 到 vendor/peerjs.min.js（93KB），index.html 引入。
- net.js 全面重写：RoomClient（开房间=6位码做PeerID直连）、MatchClient（匹配=listAllPeers找同模式玩家+mesh全互联）。iceServers=[] 纯直连。
- main.js 适配：移除全部 /api/ fetch 调用；开房间 roster 在 createRoom/waitForJoiner 或 joinRoom 成功后构建假 roster；匹配 roster 由 findMatch 返回的 peerId 数组按字典序分配 team/slot。
- 新增部署说明.md（纯静态，直接上传 Pages）。
- 7 模块 node --check 通过；DOM id 交叉校验无缺失。

## 2026-08-09 九次改进：删握手服务器+匹配，改手动SDP直连
- 用户决定删除所有握手/信令服务器和匹配模式，改用手动交换 SDP 码联机。
- 删除 vendor/peerjs.min.js、matchClient/RoomClient/全部 PeerJS 代码、匹配模式按钮和面板。
- net.js 精简为 Net 类（RTCPeerConnection + 双 DataChannel + createOffer/acceptAnswer/join），iceServers=[]。
- index.html 菜单改为三个按钮：练习模式/创建房间/加入房间；主机面板（生成邀请码→粘贴应答码→连接）、加入面板（粘贴邀请码→生成应答码→复制）。
- main.js 恢复 SDP 手动交换接线，wireNet 统一路由，onPeerOpen 自动触发选图大厅。
- 部署说明.md 更新为纯静态直传。
- 7 模块 node --check 通过；DOM id 交叉校验无缺失；旧 id 残留检查全部清零。

## 2026-08-09 十次改进：开房间恢复规模选择 + mesh 多人
- 主机面板恢复 1v1/3v3/5v5 规模选择卡片（hostSizeChoice）。
- 主机流程改为 mesh 多人：选规模→生成邀请码→好友粘贴生成应答码→主机粘贴连接→重复直到人齐→进选图大厅。
- main.js 用 nets{} 字典管理多 Net 连接；wireNet(n, peerKey) 绑定每个连接；onPeerOpen 自动加 roster 并检查人齐；netBroadcast/netSendState 遍历所有 nets。
- buildAllPlanes/nextRound 的 teamSize 改为 netSize/2（不再用 MODE_SIZE）。

## 2026-08-09 简化练习模式
- 练习模式去掉规模选择，固定 1v1（玩家 vs 1 个 AI）。
- 点练习→直接选地图→开局，去掉中间的规模选择步骤。
- startPractice 精简为只建 1 个敌方 AI。
