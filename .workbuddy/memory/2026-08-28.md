## 主题改为主站风格（第二十六轮，用户改主意：弃acofork，模仿4365754.xyz）
- 抓取 4365754.xyz 内联样式提取风格：body #f0f2f5 + Noto Sans SC + #333 文字；主色 #2563eb/#38bdf8 蓝色渐变；玻璃拟态 rgba(255,255,255,.72)+blur(14px) saturate(1.8)；圆角 12px；柔和蓝色阴影
- 全部替换：米白底→浅灰底、荧光黄绿→蓝色渐变按钮+白字、卡片选中态蓝描边+光圈、logo 黄绿→蓝渐变、sub 高亮蓝、verTag/kbd 蓝色、光晕改蓝；theme-color #f0f2f5
- 残留检查 0；HUD/触屏保持深色
## 沉浸式 3D 展示菜单（第二十七轮，v1.4.0 布局重构）
- 主菜单从居中面板 → 沉浸式：进入页面看到 3D 展示场景（空战=飞机停跑道面向相机；防空车=防空车停跑道+天上飞机盘旋），overlay 透明化(0.14)露出 3D
- 顶部：帮助药丸(左)+logo(中)+版本(右)；玩法药丸一行(menuGmTabs，切换 setMenuGm → showMenuScene 换场景)；底部三个大药丸(练习/创建/加入)
- 二级面板(practice/host/join/mapLobby)收进 menuBoxBody(默认隐藏，showMenuBox 打开)；操作说明移入 helpPanel 玻璃弹层(btnHelp/btnCloseHelp)
- 展示实体：menuPlane/menuAAGun/menuFlying + updateMenuScene(螺旋桨/炮塔扫描/盘旋)；菜单相机低角度近景；beginMatch 清理 clearMenuProps
- 玻璃设计：药丸(blur+saturate+白描边)、大药丸蓝渐变、ghostPill 透明蓝描边
## 展示场景修正（第二十八轮）
- 空战菜单：island → 复用 base 机场；飞机加起落架（addLandingGear：前轮+双主轮+支柱，buildPlane 无起落架）；位置 y=1.3 轮子着地；相机斜 45°俯视机头（camera(0,24,18) lookAt(0,1.8,-8)）
- 防空车菜单：天上飞机从绕圈盘旋 → 静止悬停（位置朝向固定，只有螺旋桨转）
- 所有按钮去渐变填充改统一毛玻璃：button/bigPill/ghostPill rgba(255,255,255,.55-.6)+blur+saturate+白描边+深色文字；gmPill.on 白玻璃选中态；蓝色渐变清零
## 菜单轨道视角+展示修正（第二十九轮）
- 防空车模式：天上飞机 y42→26 + 相机目标抬高(0,13,6)、yaw=PI、dist58 → 防空车和天上飞机同框
- 空战模式：相机改轨道斜上方（yaw0/pitch0.4/dist28 → 俯角~23° 机头前上方，不再是正上方）
- 新增菜单轨道视角：鼠标拖动旋转（yaw/pitch，pitch 限 0.12-1.25 防穿地）、滚轮缩放（dist 14-85 防贴脸）、右上角倍数标签(menuZoomInfo)、切换场景重置视角；视角始终 lookAt 主体
