# 2026-09-01 工作日志

## 房主小世界架构收尾 + 选阵营适配 + v2.0.0

### 完成内容
1. **残留 hit 消息清除**（旧 mesh 定向伤害协议）：
   - `applySplashToAAGun`（防空车溅射）：改为房主端 `applyPeerDamage` 权威结算，非房主端只做本地预判特效
   - 友军相撞：非房主上报 `crash{target,dmg:12}`，房主权威扣双方；房主端直接 `applyPeerDamage`
   - 全部 `t:'hit'` 引用清零

2. **crash 消息改造为碰撞权威结算**：
   - crash 增加 `dmg`（友军轻撞，双方各扣）/ `fatal`（敌机互撞/撞防空车，同归于尽）语义
   - 新增 `crashPairs` Map + `markCrashPair(a,b)` 去重（1.5s 窗口）：碰撞双方各自检测并各自上报，房主只权威结算一次
   - 房主端 tick 碰撞检测段同样调用 markCrashPair（先本地结算的，对方上报的同碰撞自动忽略）
   - case 'crash' 处理含房主自己被撞（selfDestruct + peerDie(sender)）

3. **选阵营星型转发链修复**（关键 bug）：
   - `checkReq`/`checkOk`/`checkBad` 此前缺 `target` 字段，hostRelay 只转发带 target 的定向消息 → 核对消息断链，核对退化成"超时信任名单"
   - sendNextCheck 与 checkOk/checkBad 补 `target`；hostRelay 改为 `d.target || d.to`
   - 选阵营其余流程（pickStart/pickCount/pickList/ready/pickReset/pickReject）经核查在星型下已正确：pick 定向房主、广播类由房主发出、ready 用 pickWaitingReady

4. **非房主端 netSize 同步**（新发现 bug）：`case 'roster'` 增加 `netSize = roster.length`，修复 3v3/5v5 非房主出生点布局重叠（spawnPos 用 netSize/2 分布 x 坐标）

5. **版本**：`GAME_VERSION = 'v2.0.0'`；缓存版本 1406 → 1407（index.html + main.js 7 处 import）

### 验证
- 全部 js `node --check` 通过
- 8890 服务器重启（杀旧进程 14816 后启动），全部资源 200
- 删除临时脚本 _rebuild_menu.mjs

### 延迟补偿参数（房主端命中判定）
- 飞机位置上报 >350ms 过期 → 跳过命中
- 防空车上报 >400ms 过期 → 跳过命中

---

## 架构演示图（host-mini-world-arch.html）

### 用途
- 自包含 HTML 演示"房主小世界"架构（v2.0.0）
- 浅色 iOS 果味玻璃拟态风格；与游戏本体同源
- 可切换 1v1/3v3/5v5 观察连接数；可暂停/调流速；点击玩家节点高亮

### 拓扑布局（已迭代三轮，最终版本）
- SVG viewBox 1100×640；房主中心 CX=550, CY=380, 半径 HOST_R=58
- 玩家节点：1v1 在房主正下方 y=CY+215=595；多玩家用椭圆上半弧分布（ORBIT_RX=430, ORBIT_RY=230，等 x 间距）
- 玩家间虚线 + 玩家与房主双向箭头线（upd 蓝上行 / sync 绿下行）
- 连线中点标签（"upd 20Hz"、"sync 20Hz"、"fire · blt · ping"）按连线法向偏移 + 白描边提高可读性

### 关键面板
1. 房主小世界内部（权威裁决/延迟补偿/状态同步器）
2. 消息协议表（upd/fire/sync/blt/rEnd/matchEnd/ping/check）
3. 本地预判 vs 房主权威
4. 掉线处理（重型天然简单）

### 静态模式
- URL `?static` 禁用粒子动画（便于截图/分享）

### 验证
- playwright + 系统 Edge 截图渲染：1v1/3v3/5v5 节点/气泡/镜像正常生成（9/73/10）
- PNG 1280×1677 fullPage，SVG 区域 y=162~847
- 临时验证脚本 _arch_shot.js / _arch_check_pos.js / _arch_size.js 已清理

### 关键 bug 教训
- 节点布局早期版本用 `cos/sin` 圆弧均匀分布，导致 5v5 中间玩家堆叠在房主正上方
- 修复改用椭圆上半弧 + 等 x 间距，视觉上分散且不重叠

---

## v2.1.0：本地预判+权威回溯 / 定向分配阵营 / 架构图更新

### 用户需求（原话要点）
1. 伤害/碰撞实时性强 → 本地预判：本地击中先扣血+上报房主，房主算出未命中且没扣血 → 立即回溯血量
2. 死亡实时性不强 → 不做本地预测（撞海撞地除外）
3. 架构图 HTML 里玩家与玩家之间的虚线 → 删除（误导为存在连接）
4. 选阵营：删除校验程序；改为房主定向发送队友/对手（对玩家A：B是队友、C是对手）
5. 总原则：按市面上的游戏做，只是服务器换成房主

### main.js 改动
- 预判对账基建：myFireSeq/firePred{fid:{id,dmg,t}}（5s过期）、recordFirePred()、foeBulletMiss()
- spawnBullet/fireGun/fireGunAAGun 返回子弹对象（打 fid 标记）
- 非房主开火上报 fire 带 fid；房主 spawnBullet 记 fid/fFrom/fHit，子弹未命中消失时发 dmgReject{fid}
- 非房主命中预判：pl.hp -= dmg + pl.predTotal += dmg（飞机/防空车/溅射 applySplashToAAGun 同步支持）
- sync 权威对账：pl.lastAuth 维护 + hp = 权威 - predTotal（已确认预判自动扣除）；nextRound 清零 predTotal/lastAuth/firePred
- dmgReject 处理：立即恢复血量（hp = lastAuth - predTotal）+ 提示"判定未命中 · 已回溯"
- 被打预判：applyMeDamage(dmg, pred=true) 不触发本地死亡（死亡等 sync）
- fatal 碰撞（敌机/撞防空车）：房主 peerDie(id)+selfDestruct()（权威判双方）；非房主只上报 crash fatal 等 sync
- 环境伤害：撞地8/出界3 → envDmg 上报房主 applyPeerDamage 权威结算
- 修复顺手 bug：房主开火广播 blt（原来广播 fire 非房主忽略=看不到房主子弹）；blt 带 from，开火者收到自己的 blt 跳过
- 选阵营定向分配：broadcastPickCount 选满后对每个玩家 netSendEventTo pickList{team,slot,m,f}；玩家端 upsertRoster 落位、直接 ready；删 checkReq/Ok/Bad/pickReset/startPickVerify/sameTeams/myVerify*；hostRelay 简化为防御性转发
- 版本 v2.1.0；缓存 1407→1408（main.js 7处 + index.html 1处）

### host-mini-world-arch.html 改动
- 删除玩家间虚线 + "虚线=无直连"底部文字（dashedLines:0 验证）
- 消息协议表：新增 dmgReject（否决回溯）、envDmg/crash、pickList（定向分配）行；删 check/pick 核对行
- "本地预判 vs 房主权威"面板 → "本地预判 + 权威回溯（市售网游模式）"5 条规则
- 顶栏 v2.1.0 + 副标题"本地预判 + 权威回溯"
- **修复 defs 被清空 bug**：render() 的 svg.innerHTML='' 会清掉 <defs> 渐变 → 房主核心节点无填充消失；新增 <g id="topoG"> 绘制图层，清空只清 topoG
- playwright 验证：nodes:5 / dashedLines:0 / mirrors:6 / hasDmgReject:true / hasPickList:true / noCheckChain:true；截图确认房主核心正常渲染

### 验证
- node --check：main.js/net.js/ai.js/plane.js 全部通过
- checkReq 等仅剩 1 处注释引用；v=1407 无残留
- 8890 服务器 200

## v2.1.1：网络同步四问修复（2026-09-01 晚）

用户提出四问：①开枪会不会被旧快照拖回过去位置 ②预判扣血被旧快照回满再扣回 ③命中上报有没有带目标位置 ④打的是不是敌人过去的状态。

### 结论
- Q1 不会：自己位置/子弹本地权威；别人位置 tPos+tQuat 目标点 lerp/slerp 渐进追踪，旧快照只滞后不闪回
- Q2 我打别人已有对账；**我被打是真 bug**：sync 自己分支 me.hp=it.hp 直接覆盖 → 旧快照回满再扣回
- Q3 没带：fire 只有 o/d/fid，判定全靠房主小世界最新位置（偏差 ≈ RTT/2 × 敌速）
- Q4 承认滞后 ~1 RTT，只有插值无延迟补偿

### 修复（main.js，v=1409）
- 被打预判对账：me/meAAGun 加 predTotal/lastAuth/predT；applyMeDamage(pred) 记账；sync 自己分支用对账公式（不再直接覆盖）；processNetReconcile 400ms 超时回滚（sync 到达自愈）
- hitRep 信任命中：非房主命中时上报 {fid, id, p(命中瞬间目标位置), dmg}（飞机/防空车/溅射三处）；房主 b.trust 登记，子弹到达上报点 22m 内（且上报点距目标现位置<60m 防瞬移谎报）→ trustDamage 按上报结算（含 bot/房主自己）
- 延迟否决：foeBulletMiss 改入 pendingRejects 队列（220ms 等 hitRep，过期才发 dmgReject），消除"先否决后确认"抖动；fireBullets{} 存 fid→子弹映射
- nextRound 清 fireBullets/pendingRejects/me 对账字段；processNetReconcile 挂在 20Hz 状态同步节拍

### 验证
- node --check 全部 8 个 js 通过；v=1409 替换 main.js 7 处 + index.html 1 处；GAME_VERSION v2.1.1

## v2.1.2：通信/状态对齐巡检 + 选阵营掉线处理（2026-09-01 深夜）

用户要求：再找通信/状态对齐 bug；问选阵营时掉线怎么算。

### 发现并修复的 bug（v=1410）
1. **sync 去重键混用（严重）**：sync 走 state 通道（stateSeq 计数）却用 lastEventSeq 去重——两计数器独立 + state 通道无序，无序快照先到会把后续 event（blt/go/hitRep/dmgReject）全部压掉 → 改独立 lastSyncSeq
2. **pingMs 时钟基准错误**：房主广播自己 performance.now()，两端时钟基准不同 → ping 显示垃圾值 → 改 ping-pong（非房主发 ping、房主回 pong、非房主本地算 RTT）
3. **pickWaitingReady 阶段掉线卡死（用户问的场景）**：定向发放名单后 pickPhase=false/pickWaitingReady=true，onPeerClose 只查 pickPhase → 此阶段掉线房主永远等不够 ready。修复：条件改 pickPhase||pickWaitingReady，双向清理并广播 pickCancel
4. **掉线后 netSize/roster 不同步**：房主端 removePeerLocal 后 netSize 不变 → 重选时 ready 阈值含掉线者再次卡死。修复：netSize=roster.length + broadcastRoster() 重发
5. **hitRep 迟到双结算**：dmgReject 已发出后 hitRep 才到 → trustDamage 再扣一次。修复：rejectedFids Map（5s 过期），hitRep 检查忽略
6. **回合结束后仍接受 fire/hitRep**：加 STATE.roundOver 防护
7. **选阵营 45s 超时自动分配**：挂机/失联玩家自动分配到有空位阵营（pickStartTs + broadcastPickCount 内检查），防无限等待

### 选阵营掉线行为（修复后完整链路）
- 选边阶段掉线：房主取消本轮 → 全员回 lobby"有玩家掉线，等待重新连接"→ 人齐重开 pickStart
- 等就绪阶段掉线：同上（原版卡死已修）
- 房主本人掉线：非房主收 onClose → 凑人阶段提示后返菜单；对局中直接结束比赛判房主队伍负
- 对局中普通玩家掉线：视为阵亡触发回合结算；整队掉光直接结束比赛

### 验证
- node --check 全部通过；v=1410（main.js 7 处 + index.html 1 处）；GAME_VERSION v2.1.2

## v2.1.3：hitRep 携带字段确认 + fid 跨玩家撞号修复（2026-09-01）

用户问：本地命中敌人后上报的信息包含自己的 id 和对方的 id 吗？
- 答：对方 id 有（hitRep.id）；自己 id 由 netSendEventTo 自动附带的 sender 字段携带（房主端 rid = d.sender || peerId 解出）
- 顺带发现真 bug：fid 是各玩家独立计数（myFireSeq），房主端 fireBullets/rejectedFids 只用 fid 做 key → 玩家 A 的 5 号弹与玩家 B 的 5 号弹互相覆盖（A 的 hitRep 可能登记到 B 的子弹）
- 修复：fireBullets / rejectedFids 全部改 "发送者id:fid" 复合键（fire 登记 / foeBulletMiss 删除 / 信任命中删除 / hitRep 查找 / pendingRejects findIndex 加 from 校验）
- 版本 v2.1.3，缓存 v=1411；node --check 通过

## v2.1.4：凑人阶段掉线补漏 + 全流程走查确认（2026-09-01）

用户两问：①凑人阶段掉线会怎样 ②普通玩家全流程走查（上报/显示/命中/纠错/掉线/坠毁/比分/胜利是否每步正确）

### 走查结论：流程整体成立，补 2 个凑人阶段缺口
1. **非房主凑人阶段卡死漏洞**：team 消息未到（hostPeerId 未赋值）时房主掉线，onClose 匹配不上 → 非房主永远卡大厅。修复：非房主 !STATE.started 时任何 onClose = 房主掉线 → banner + 返菜单（星型拓扑下非房主唯一连接就是房主）
2. **房主"已连接 x/y"提示不刷新**：凑人阶段玩家掉线后 updateHostStatus 未调用。修复：!STATE.started 时调用

### 全流程机制确认（均已实现）
- 上报独立不混淆：每条消息带 sender，seq 按 rid 分键去重，fid 已改复合键
- 看到对方：位置 20Hz sync+lerp（~1RTT 平滑滞后）；子弹 blt 重建
- 命中即时扣血：本地预判瞬时；对象正确（本地实体 keyed by peerId + hitRep.id + 复合键）
- 纠错：dmgReject 220ms 延迟否决 / predTotal+lastAuth sync 对账 / 被打 400ms 超时回滚
- 掉线=消失并按阵亡结算（优雅断开瞬时；拔线类依赖 ICE 检测需数秒）
- 坠毁：撞海 y<3.5 / 撞地 collideTerrain 本地即时判死 + envDmg 上报权威
- 比分一致：房主权威 s0/s1 经 rEnd+sync 下发，syncScoreFromWorld 按本地方队换算
- 胜利：WIN_ROUNDS=3，settleRound→matchOver→matchEnd 广播
- 版本 v2.1.4，缓存 v=1412，node --check 通过

## v2.1.5：同时击杀判定确认 + 实体复用 bug 修复（2026-09-02）

用户两问：①两人同时击杀怎么判定 ②有没有实体复用导致的问题

### Q1 结论（无改动，链路正确）
- 房主权威按子弹模拟顺序逐发结算：先死者触发 checkRoundEnd → 0.8s 队伍淘汰宽限窗；窗内对手也被击杀 → a0/a1 全 0 → settleRound('draw') 平局不计分
- 窗口过后才落地的"复仇弹"因 STATE.roundOver 防护不结算 → 先死者负
- 飞机相撞互杀：markCrashPair 去重只结算一次 → 双亡 → 平局
- 全端一致：非房主死亡由 sync it.al 权威推送，预判不判死
- 小提示：非房主 killCount 是本地预判击杀数，被否决时不回退（统计性偏差，非血量错误）

### Q2 修复（两个真 bug，v=1413）
1. **弹池复用残留状态（严重）**：spawnBullet 复用 inactive 子弹但不重置 fid/fFrom/fHit/trust → 上一发的否决会发给错误玩家/序号、trust 误登记到新弹道。修复：spawn 时全部清零
2. **弹池容量不足（多人局）**：120 发 < 5v5 需求（~380 发并发），打光后 spawnBullet 返回 null → 子弹不可见、无法模拟上报。修复：扩容 480
3. lastState 跨回合：回合重启间隔 >3.2s，必然已过期，首次 upd（≤50ms）即恢复，无需处理
- 版本 v2.1.5，缓存 v=1413，node --check 通过

## v2.1.6：架构深挖——回合推进缺失 + 弹道参数不一致（2026-09-02 晚）

用户要求继续找架构 bug。发现两个重量级问题：

### Bug 1（头号）：case 'round' 处理器缺失
- nextRound 广播 {t:'round', rn}（742 行）但全文件无接收 case → 非房主第二回合开始时实体永不复位（me/planes/aaguns 的 alive/visible/hp/位置），第一回合死亡后只能当幽灵，roundOver 虽由 sync over 字段恢复但实体状态单向置死
- 修复：nextRound(targetRn) 支持指定局号；新增 case 'round'（非房主 d.rn > STATE.roundNum → nextRound(d.rn)，幂等）；sync 的 rn 分支作兜底（丢包/晚到时同样触发，幂等条件防重复）
- 竞态核对：'go'→beginMatch→非房主自nextRound(1)；host round rn=1 到达 1>1 false 跳过 ✓；rematch roundNum=0 → 同理幂等 ✓；event 通道有序保证 'go' 先于 'round' ✓

### Bug 2：弹道参数跨端不一致（速度/伤害/溅射）
- 飞机子弹本地 = fighter.speed+520（巡航 615），fire/blt 接收端固定 560 → 房主权威弹道慢 ~9%，300m 外偏差 20m+，trust 22m 窗口盖不住 → 远距离误判 miss 误回溯
- AAGun 子弹：本地 520 速度/AAGUN_DMG(4) 伤害，host 'fire' 按 560/BASE_DMG 模拟 → 防空车权威伤害 2.5 倍
- aavs 飞机溅射弹：本地 splash=true，host 'fire' 丢失 splash → 权威溅射伤害丢失
- 修复：fire/blt 携带 s(速度)/dm(伤害)/sl(溅射)（bulletNetParams 从实际 bullet 读），fire handler/blt handler/4 个开火点全部对齐；560 仅剩兜底默认
- 版本 v2.1.6，缓存 v=1414，node --check 通过

## v2.1.7（本轮：matchEnd 视角 + 非房主死亡上报 + 回合结果提示）

用户指令：继续找 bug，直到重大 bug 完全被解决为止。

### 修复 1：matchEnd 胜负视角 bug（重大）
- 根因：matchOver 以房主本地视角 `STATE.myScore > STATE.foeScore` 计算 win 并广播 {t:'matchEnd', win}；非房主 `applyMatchEnd(d.win)` 直接使用 → 房主方胜利时非房主也显示"最终胜利"（双方都看到胜利）
- 修复：case 'matchEnd' 用房主推送的 s0/s1 按本地阵营换算 `iWon = myTeam===0 ? s0>s1 : s1>s0`（比分缺失兜底取反），并同步 syncScoreFromWorld 保证 matchSub 比分一致

### 修复 2：非房主本地死亡不上报（重大，永久幽灵分歧）
- 根因：非房主 selfDestruct/selfDestructAAGun（本地 hp≤0 由 envDmg 累计 / 坠地 y<3.5 / 防空车出海>430 触发）只改本地状态不通知房主。若本地 hp 因"未确认预判+环境伤害"混合提前归零而房主随后否决射击 → 权威 hp>0 永不下发 al:false → 本地死亡/房主认为存活的永久分歧，回合卡死
- 修复：两处 selfDestruct 增加非房主 `netSendEventTo(hostPeerId, {t:'died', id})`，房主 peerDie 权威对齐（与 envDmg 信任级别一致）
- 附带收益：坠地/出海原依赖 20Hz 不可靠 upd 抽样判定（丢包则权威不判死），现在本地判定直达权威

### 修复 3：非房主无回合结果提示（视觉缺口）
- 根因：settleRound（横幅+音效）仅房主/单机执行；非房主 rEnd 只同步比分，3.2s 回合间隙零反馈
- 修复：rEnd 广播携带 w(胜方队伍，-1平局)；非房主据 w 显示 回合胜利/回合失败/双方同归于尽 横幅+音效，lastREndShown 去重防重复

### 核实无问题项
- checkBulletHit/checkAAGunHit：segSphere 线段扫掠，高速子弹无穿透
- hitRep 里 ag.id：非房主 aaguns 条目均由 makeAAGunUnit 设置 id（bot 条目仅房主持有，不发 hitRep）
- pingMs：房主保持 -1 → HUD 显示空，无 bug
- rematch/go 时序：event 通道可靠有序 + nextRound(targetRn) 幂等（rn 不超前过滤），双发/乱序均收敛
- matchEnded 后 sync：rn/over 幂等，自愈
- trustDamage 覆盖 房主自己/bot/远程玩家 全部分支
- dmgReject/sync 对账/400ms 超时回滚 三层纠错闭合

版本 v2.1.7，缓存 v=1415，node --check 全部 8 个 JS 通过。
