# 2026-09-05 下午 前置落地页 v2.3.7

## 新增前置落地页（index.html 内 #landing + js/landing.js）
- Hero：SVG 手绘海报（天空渐变/太阳光晕呼吸/三层云漂移/三机编队浮动+流动尾迹/光束脉冲/海面城市山峦剪影/飞鸟）
- SKY DUEL 打字机效果：逐字 span+chIn 弹入动画，渐变裁剪必须放每个 .ch 自身（父级 background-clip:text 会被子级 transform 动画破坏成色块——本 round 踩坑）
- 标题斜体 skewX(-6deg) + clamp 字号；下方小字简介 + "开始游戏"按钮（渐变流光+扫光动画）
- 分区：01 三地图卡（SVG 缩略）→ 02 两玩法卡 → 03 无服务器联机深蓝区（传统服务器 vs 房主小世界拓扑 SVG，连线 dash 流动+节点辉光）→ 数字滚动条 → 页脚
- 滚动动效：IntersectionObserver reveal（stagger d1/d2/d3）、hero 视差（文案上移淡出）、数字 count-up、卡片 hover 抬升
- 开始过渡：leaving 类（模糊缩放淡出 + 径向白光 flash）→ body.playing → menu 子元素级联进场（menuTop/GmBar/Bottom stagger）
- landing z-index 500（菜单 overlay 是 30）；touch-action:pan-y 保内部可滚

## 版本
- v2.3.7；index.html ?v=1430；新增 js/landing.js（无依赖，先于 main.js 加载）

## 教训
- agent-browser 只装在 sandbox bash 的 npm prefix（PowerShell 找不到）；Chrome headless 直跑被 sandbox 拦（Sandbox cannot access executable + GPU context 失败）
- 本轮工作环境：bash 通道长时间 SIGTERM 全挂（连 echo/pwd 都不行），PowerShell 可执行但 native exe stdout 丢失 → 验证受阻
- 打字机 headless 验证：eval 数 .ch 数量 = 8 确认 DOM 注入成功（视觉待用户确认）
