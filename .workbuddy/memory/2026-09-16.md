# 2026-09-16

- 完成安卓版与网页版联机对齐的收尾修复并编译出 APK（复制到根目录 苍穹对决.apk）。
- Game.java：NetEv 改 float[] 载荷；修复 updateBullets 非法三元表达式；spawnPeerBullet 补 baa/bulletAuthDmg；host 物理命中/溅射命中补 bfHit+台账清理（防 hitRep 双结算）；guest 溅射近失补 hitRep 上报（距离衰减公式同 web applySplashToAAGun）；splashBoom 加子弹归属判断（我的弹只溅对方车）；AA 机枪弹改无溅射（web fireGunAAGun 不带 splash，aavs 机炮弹才有）；damageMe/trustDamageMe/被打预判/回滚改为只结算当前实体（防空车 HP120≠飞机 HP100，修复误判死）；guest 环境伤害可致死→selfDestruct（同 web applyMeDamage(3)）；netStarted 防重复开局；信任分支加 myAlive+!roundOver 守卫。
- Net.java：修复 6 处尾随大括号产生的非法 JSON（sendUpdPlane/sendUpdAA/sendSync/sendDmgReject/sendEnvDmg/sendDied）。
- Renderer.java：新增 drawAAGun（body/track 随车体、dark/armor 随炮塔）；onDrawFrame 检测 mapType 变化重建 World。
- HudView.java：比分/比分点改 scoreMy()/scoreFoe()（阵营视角）；血条按飞机/防空车切换；敌标记兼容防空车；联机面板新增房主开局地图切换按钮（海岛→都市→军事基地）。
- 删除 21 个临时文件（根目录 _proto/_scan 系列 + 安卓版 _audit/_b23~_b33/_fix 系列等）。
- 跨端协议要点：q 数组 [x,y,z,w]；upd/sync 带 sender+seq；matchEnd.win 为房主视角；web blt 过滤 d.from===myPeerId（安卓 host 发 from:"" 兼容）；host 收 guest fire 会回发 blt(from:rid)。

- 用户质询 kstat 缺口后补齐：Game.java 新增 killLedger 击杀台账（recordAuthKill 归因：房主子弹/信任命中/溅射=开火者，环境/自毁/出界=null，fatal 碰撞互为击杀者；开局/重赛/stopNet 清零）+ hostDamagePeer 统一权威扣血入口 + 3s 定向 sendKstat；Net.java 新增 sendKstat/onKstat（kstat 报文 {t:'kstat',k,d}）；加入方 myKills 权威覆盖（HUD 暂无击杀计数显示）。重新编译并更新根目录 APK。
- 结论记录：hitRep/dmgReject 与 sync 全实体（1v1=2 实体）在此前轮已完成；kstat 本轮补齐。

- **架构重大转向：安卓版从原生 GLES 改为 WebView 架构（v0.3.0）**。用户真机试玩后判定原生版"和网页版没有一点关系，可以说是两个游戏"，明确指示改为 WebView 打包网页版本体（删落地页/sitemap/robots，帮助改安卓特色版，加设置）。
  - assets/web/：play/index.html 安卓改造版（删 SEO/og/JSON-LD/favicon；"首页"按钮→"设置"；帮助内容安卓化；新增设置面板 glassPanel；路径 ../→./；?v= 全部 8001）+ js 8 个（无 landing.js）+ vendor/three.module.min.js（Sky.js 无引用不打包）
  - [ANDROID] 补丁 3 处（同步网页版更新时必须保留）：main.js GFX 对象读 sd_settings(shadows/pixel) + TOUCH_SENS 读 touchSens；audio.js 顶部 VOL_DEFAULT 读 vol + master.gain 两处引用
  - MainActivity.java 重写为 WebView 宿主：WebViewAssetLoader(https://appassets.androidplatform.net/assets/ → 安全上下文 WebRTC 必需)、DOM storage、免手势媒体、onPermissionRequest 授麦克风、LOAD_NO_CACHE、沉浸式全屏、返回键双击退出
  - 原生 13 个 .java 移至 安卓版/native-backup/com/skyduel/game/（不参与编译）；build.gradle 删 webrtc-sdk 加 androidx.webkit:1.11.0，versionCode 3 / versionName 0.3.0；gradle.properties 改 android.useAndroidX=true（首次引 AndroidX）；Manifest 加 RECORD_AUDIO/MODIFY_AUDIO_SETTINGS
  - 设置面板：菜单左上角"设置"→ 灵敏度/画质精度/阴影/音量，存 localStorage('sd_settings')，"应用"后 location.reload() 生效（const 顶层读取）
  - 剪贴板确认：网页版用 textarea.select()+execCommand('copy')，WebView 原生支持，无需 JS 桥；isTouch=pointer:coarse 自动触屏模式，lockPointer 触屏下 return（无需 Pointer Lock）
  - APK 783KB（原生版 31MB，去 webrtc-sdk），已复制根目录 苍穹对决.apk；python zipfile 验证 10 个 assets/web 资源在包内
  - PowerShell 删除文件踩坑：safe-delete 钩子 trash 操作对逐文件删除报 ERR 但实际删掉；先 Copy-Item 备份再删的流程可用

- **v0.3.1（WebView 版第二轮真机反馈修复）**：①辅助瞄准回归——main.js [ANDROID] 补丁 AIM_ASSIST（0/1/2 三档，默认弱）+ aimAssistTick(dt)（开火时遍历 planes/aaguns 敌方 weakPoints._w 世界坐标，锥角 0.10/0.17 rad 内选最近，反解 Euler YXZ 得 yawT=atan2(-bx,-bz)/pitchT=asin(by)，轻推 aimYaw/aimPitch 交由飞机追赶逻辑消费；插入点=主循环 joySteerX 块后，飞机分支内天然满足存活条件；网页版无 myAlive 函数）；②帮助/设置面板加 panelHead+panelX 右上角固定✕（面板 touch-action:none 全局导致 overflow:auto 手势滚动失效是"看不到返回键"根因之一，补 .glassPanel/.menuBox/#menuBoxBody/textarea{touch-action:pan-y}）；③返回键桥 window.__sdHandleBack（settingsPanel→helpPanel→menuBoxBody 依次检查关闭返回 true；注意 menuBoxBody 显示态是 style.display=''、隐藏是 'none'）+ MainActivity onBackPressed evaluateJavascript 优先关面板否则双击退出；④菜单 3D 展示触屏补丁（单指拖拽旋转/双指捏合缩放 menuCamDist，排除 button/textarea/.menuBox/.glassPanel）；⑤全屏加固 cutout SHORT_EDGES + onSystemUiVisibilityChangeListener 重沉浸；⑥键位布局编辑器：设置→键位布局→body.layoutEdit 模式，document capture 阶段拦 touchstart/touchmove（stopPropagation 防误触游戏），拖动控件中心百分比定位（left/top+right/bottom=auto+translate(-50%,-50%) scale(s)），layoutBar 工具条滑条调 0.6–1.8 大小，存 localStorage('sd_layout')，摇杆 rect 动态读所以移动兼容；?v= 8002（index.html script + main.js 7 个 import）；versionCode 4 / 0.3.1；APK 788KB 已交根目录
