# 苍穹对决 · 部署说明

## 部署到 Cloudflare Pages

1. 打开 https://dash.cloudflare.com
2. 左侧菜单 → **Workers & Pages**
3. 点 **Create** → **Pages** → **Upload assets**（直接上传）
4. 项目名填 `sky-duel`，点 Create
5. 把项目文件夹里的所有文件拖进上传区域：
   - `index.html`（落地页，`/`）
   - `play/`（游戏页，`/play/`）
   - `articles/`（技术专栏三篇文章）
   - `sitemap.xml`、`robots.txt`
   - `favicon.svg`
   - `js/` 文件夹
   - `vendor/` 文件夹
6. 点 **Deploy**

完成。打开分配的网址即可游玩。

> 项目是纯静态文件，不需要构建过程、不需要后端、不需要数据库、不需要任何外部服务器。

---

## 本地测试

直接双击 `index.html` 即可在浏览器运行。联机测试：开两个浏览器窗口/标签页，一个点「创建房间」生成邀请码复制，另一个点「加入房间」粘贴邀请码生成应答码复制，主机粘贴应答码连接即可。

---

## 联机原理

- **创建房间**：主机点「创建房间」→ 生成邀请码 → 复制发给好友 → 好友粘贴后生成应答码 → 主机粘贴应答码 → 连接建立
- **数据传输**：握手完成后纯 WebRTC DataChannel 直连，无任何中转
- **无外部服务器**：不使用 STUN、不使用 TURN、不使用 PeerJS、不使用信令服务器。`iceServers` 配置为空数组，纯 WebRTC 直连

---

## 文件结构

```
index.html                     落地页（/）：海报 hero + 特性 + 技术专栏入口
play/index.html                游戏页（/play/）：菜单 + 联机 + 战斗
articles/p2p/index.html        文章一：零服务器联机（WebRTC 直连）
articles/host-world/index.html 文章二：房主小世界权威架构（含架构演示图）
articles/prediction/index.html 文章三：本地预判 + 权威回溯
sitemap.xml / robots.txt       SEO
favicon.svg                    图标
js/landing.js                  落地页交互
js/main.js                     主循环/菜单/输入/回合逻辑
js/net.js                      WebRTC 联机层（纯直连，无服务器）
js/plane.js                    飞机模型与飞行物理
js/world.js                    三地图（海岛/都市/军事基地）+ 碰撞体
js/aagun.js                    防空车模型与炮塔
js/effects.js                  爆炸/粒子/震屏
js/audio.js                    Web Audio 合成音效
js/ai.js                       AI 机器人
vendor/three.module.min.js     Three.js（本地）
host-mini-world-arch.html      房主小世界架构演示图（文章二内嵌）
```

> 注意：`robots.txt` 与 `sitemap.xml` 已指向正式域名 `game.4365754.xyz`；在 Cloudflare Pages 控制台把该自定义域绑定到本项目即可。
